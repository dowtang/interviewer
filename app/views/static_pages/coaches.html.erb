<section id="about" class="container content-section text-center">
  <div class="row">
    <p>Pick an industry you are interested in</p>
    <div class="col-lg-6 col-lg-offset-3 col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1">
      <%= link_to '<button type="button" class="btn btn-default btn-lg">
        <span class="glyphicon glyphicon-floppy-open" aria-hidden="true"></span> Software
      </button>'.html_safe, software_coaches_path %>
      <button type="button" class="btn btn-default btn-lg">
        <span class="glyphicon glyphicon-signal" aria-hidden="true"></span> Consulting
      </button>
      <button type="button" class="btn btn-default btn-lg">
        <span class="glyphicon glyphicon-usd" aria-hidden="true"></span>Finance  
      </button>
    </div>
  </div>

  <div>
  <%= @dow.each do |user| %>
  <% end %>
  </div>

  <div>
  <%= @dow.each do |user| %>
  <% end %>
  </div>


</section>
